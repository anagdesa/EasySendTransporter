<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditScreenTextInputLayoutStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/gradient_start_color</item>
        <item name="colorControlActivated">@color/gradient_end_color</item>
        <item name="colorControlHighlight">@color/gradient_start_color</item>
        <item name="colorAccent">@color/gradient_end_color</item>
        <item name="android:textColorHint">@color/gradient_end_color</item>
    </style>

</resources>
